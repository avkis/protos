// [START declaration]
syntax = "proto3";

package loggerpb;

import "google/protobuf/timestamp.proto";
// [END declaration]

// [START go_declaration]
option go_package = "github.com/avkis/protos/golang/logger";
// [END go_declaration]


// Logger is service for managing permissions and roles.
service Logger {
  // Register registers a new user.
  rpc Register (RegisterRequest) returns (RegisterResponse);
}

// [START messages]
message RegisterRequest {
  string message = 1; // Message to register.
  google.protobuf.Timestamp created_at = 2; // Timestamp of the registration.
}

message RegisterResponse {
  string user_id = 1; // User ID of the registered user.
}

// [END messages]